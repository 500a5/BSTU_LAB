const btn = document.querySelectorAll('.authorization.form-btn')
btn.forEach((item)=>{
    item.addEventListener('click',()=>{
        const  price = item.getAttribute('da')
        console.log(price)
    })

})
console.log(company.innerHTML)